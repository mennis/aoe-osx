README
------

To test, build using XCode and install the kext on the command line with "./install".
To uninstall, run "./uninstall".

NOTE: Because the install script sets the permissions, you will not be able build until you run the uninstall script